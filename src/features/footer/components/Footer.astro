---
import { firm } from "../../config/firm";
import { legalNav, nav } from "../../config/nav";

const { companyName, phone, address, officeHours } = firm;
---

<!-- Footer scaffold.
     Use `firm` for company, contact, address, and office hours data.
     Use `nav` for primary navigation links.
     Use `legalNav` for legal links.
     Layout, ordering, and presentation can be rebuilt as needed. -->
<footer>
	<!-- Footer should surface:
	     - brand or company identity using `companyName`
	     - address details from `address`
	     - phone details from `phone`
	     - office hours from `officeHours`
	     - primary navigation from `nav`
	     - legal navigation from `legalNav`
	     - regulatory / trust note linking to the Law Society of Ireland
	     - current year copyright using `companyName`

	     Available data:
	     - `companyName`
	     - `address.`
	     - `phone.phoneHref`
	     - `phone.display`
	     - `officeHours.display`
	     - `nav`
	     - `legalNav`

	     Layout, markup structure, and presentation can be rebuilt freely. -->
</footer>
</footer>
