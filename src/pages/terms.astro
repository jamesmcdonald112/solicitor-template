---
import { termsPageContent } from "../features/legal/content/terms";
import { firm } from "../config/firm";
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title={`${termsPageContent.title} | ${firm.companyName}`}
  description={termsPageContent.description}
  canonicalPath="/terms"
>
  <!-- Terms page scaffold.
       Render terms of use content here.
       Use `firm.companyName` where company context is needed. -->
</BaseLayout>
