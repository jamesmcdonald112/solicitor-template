# Architecture

- Organize by feature.
- Keep related files together.
- If a file gets too large, break it into sensible components.
- Keep route files light when possible.
- Only create shared primitives when reuse is real.
