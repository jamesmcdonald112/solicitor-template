Read these files in this exact order before writing any code:

1. view .claude/skills/frontend-design/SKILL.md
2. view src/config/firm.ts
3. view src/config/nav.ts
4. view src/config/home.ts
5. view src/styles/tokens.css
6. view src/styles/themes/theme-cream-navy.css
7. view _system/STAGE-3-DESIGN-BRIEF.md

Then read the component structure section of the brief carefully.

You are building Session 1 — Foundation for a premium Irish solicitor website.

COMPONENTS THIS SESSION:
- Preloader
- Button
- Section
- Container
- Navbar
- Hero
- Footer

CONTENT SOURCE:
- src/config/firm.ts — firm name, logo, address, phone, email
- src/config/nav.ts — navigation links
- src/config/home.ts — hero heading, subheading, CTA text

RULES:
- Read config files before building any component that displays content
- Use real data from config — no placeholders
- CSS in <style> block inside .astro — no sibling .css files
- GSAP gets a sibling .gsap.ts file only for animated components
- No raw values — tokens only
- Follow file structure in the brief exactly
- After building all components, wire them into src/pages/index.astro
  so the full homepage foundation is visible in the browser

Build all components now.