# STAGE 2 — BUILD THE DESIGN TOKEN SYSTEM

## Context: What This Project Is

This is an Astro 6 template for building professional solicitor websites in Ireland. It uses plain CSS (no Tailwind), CUBE CSS methodology, GSAP for animations, Zod + Resend for forms, and TypeScript throughout. The goal is a single template that can be re-themed per client by changing only a theme file.

Stage 1 (cleanup) has already been completed:
- Tailwind has been removed
- Contact form has been moved to `src/features/contact-form/`
- GSAP is installed
- The thank-you page build error has been fixed

This stage builds the complete design token system. Do not touch any TypeScript, any page files, any config files, or any component logic. This stage is CSS only.

---

## Architecture Overview

The system uses three layers. This is non-negotiable and must be respected in every file you touch.

```
LAYER 1 — PRIMITIVES
  Raw values only. Named by what they ARE.
  Live in: src/styles/themes/theme-[name].css
  Rule: Never referenced directly in components. Only semantic tokens consume them.

LAYER 2 — SEMANTIC
  Named by what they MEAN and where they go.
  Live in: src/styles/tokens.css
  Rule: Always reference a primitive via var(). Never a raw value.

LAYER 3 — COMPONENT
  Scoped overrides for a specific component.
  Live in: the component's own <style> block or CSS file.
  Rule: Always reference a semantic token. Never a primitive or raw value.
```

---

## Naming Convention

All tokens use kebab-case. No exceptions.

### Pattern for primitives:
```
--[colour-family]-[brightness-scale]     e.g. --navy-900
--[size-scale]-[number]                  e.g. --size-4
--[font-stack]-[name]                    e.g. --font-stack-serif
```

### Pattern for semantic tokens:
```
--[category]-[subcategory]-[variant]     e.g. --color-text-muted
```

### Categories (use these exact words, no synonyms):
- `color` — not "colour", not "clr"
- `font` — not "type", not "typography", not "text"
- `space` — not "spacing", not "gap", not "margin"
- `radius` — not "border-radius", not "rounded"
- `shadow` — not "box-shadow", not "elevation"
- `duration` — not "timing", not "time"
- `ease` — not "easing", not "transition"
- `width` — for layout max-widths only

---

## STEP 1 — Create the Default Theme File

Create `src/styles/themes/theme-cream-navy.css`

This is the default theme and the template for all other themes. It defines Layer 1 only — raw primitives. Comments are mandatory and must be kept exactly as written — they guide future Claude sessions.

```css
/*
  THEME: Cream Navy (Default)
  ─────────────────────────────────────────────────────────────────────────────
  LAYER 1 — PRIMITIVES ONLY.
  These are raw values. They are never used directly in components.
  Only semantic tokens in tokens.css consume these via var().

  TO CREATE A NEW CLIENT THEME:
  1. Duplicate this file and rename it theme-[client-name].css
  2. Change the primitive values below to match the client's brand
  3. Import the new theme file in global.css instead of this one
  4. Do not change tokens.css — semantic tokens remain identical across themes
  ─────────────────────────────────────────────────────────────────────────────
*/

:root {

  /* ── COLOUR PRIMITIVES ───────────────────────────────────────────────────
     Colour family + numeric brightness scale (50–900).
     50  = lightest tint
     900 = darkest shade
     Not every step needs a value — only define what the system uses.
  ──────────────────────────────────────────────────────────────────────── */

  /* Navy — primary brand colour family */
  --navy-50:  #eef1f7;
  --navy-100: #d5dce9;
  --navy-200: #aab8d3;
  --navy-300: #7e94bc;
  --navy-400: #5371a5;
  --navy-500: #2a4a7f;
  --navy-600: #1e3a6e;
  --navy-700: #1a3460;
  --navy-800: #142850;
  --navy-900: #0f1f3d;

  /* Gold — accent colour family */
  --gold-50:  #fdf8ea;
  --gold-100: #f8eecc;
  --gold-200: #f0da99;
  --gold-300: #d4b84a;
  --gold-400: #c9a84c;
  --gold-500: #b8960c;
  --gold-600: #9a7d0a;
  --gold-700: #7c6408;

  /* Stone — neutral / surface colour family */
  --stone-50:  #fafaf9;
  --stone-100: #f5f3f0;
  --stone-200: #ede9e4;
  --stone-300: #ddd8d0;
  --stone-400: #b8b0a4;
  --stone-500: #8a8278;
  --stone-600: #5c564e;
  --stone-700: #3d3832;

  /* Absolute */
  --white: #ffffff;
  --black: #0a0a0a;

  /* State colours */
  --red-500:   #dc2626;
  --red-100:   #fee2e2;
  --green-500: #16a34a;
  --green-100: #dcfce7;
  --amber-500: #d97706;
  --amber-100: #fef3c7;


  /* ── FONT STACK PRIMITIVES ───────────────────────────────────────────────
     These are the only font-family values in the entire project.
     Semantic tokens --font-serif and --font-sans reference these.
  ──────────────────────────────────────────────────────────────────────── */

  --font-stack-serif: 'Cormorant Garamond', 'Georgia', 'Times New Roman', serif;
  --font-stack-sans:  'DM Sans', 'Helvetica Neue', 'Arial', sans-serif;
  --font-stack-mono:  'JetBrains Mono', 'Courier New', monospace;


  /* ── RAW SIZE SCALE ──────────────────────────────────────────────────────
     Base unit: 0.25rem (4px at default browser font size of 16px).
     Every spacing, font-size, radius, and layout token traces back to this.
     Named by their rem value for clarity.
  ──────────────────────────────────────────────────────────────────────── */

  --size-0-5: 0.125rem;  /*  2px */
  --size-1:   0.25rem;   /*  4px */
  --size-2:   0.5rem;    /*  8px */
  --size-3:   0.75rem;   /* 12px */
  --size-3-5: 0.875rem;  /* 14px */
  --size-4:   1rem;      /* 16px */
  --size-5:   1.25rem;   /* 20px */
  --size-6:   1.5rem;    /* 24px */
  --size-7:   1.75rem;   /* 28px */
  --size-8:   2rem;      /* 32px */
  --size-10:  2.5rem;    /* 40px */
  --size-12:  3rem;      /* 48px */
  --size-14:  3.5rem;    /* 56px */
  --size-16:  4rem;      /* 64px */
  --size-20:  5rem;      /* 80px */
  --size-24:  6rem;      /* 96px */
  --size-28:  7rem;      /* 112px */
  --size-32:  8rem;      /* 128px */
  --size-40:  10rem;     /* 160px */
  --size-48:  12rem;     /* 192px */
  --size-64:  16rem;     /* 256px */

}
```

---

## STEP 2 — Build tokens.css in Full

Replace the contents of `src/styles/tokens.css` entirely with the following. Every comment must be preserved exactly — they are instructions for future Claude sessions, not documentation for humans.

```css
/*
  DESIGN TOKENS — LAYER 2: SEMANTIC
  ─────────────────────────────────────────────────────────────────────────────
  These tokens give meaning to primitives. They describe ROLE, not appearance.

  RULES FOR THIS FILE:
  1. Every value must be a var() reference to a primitive — never a raw value
  2. Tokens are grouped by category. Do not mix categories.
  3. Components use these tokens. Components never touch primitives.
  4. To retheme the site, change the theme file. This file never changes.
  5. New tokens go in the correct category section below.
  6. If you cannot find a semantic token that fits, create one here — do not
     use a primitive directly in a component.

  IMPORT ORDER IN global.css:
  1. reset.css
  2. theme-[name].css   ← primitives (Layer 1)
  3. tokens.css         ← semantic tokens (Layer 2, this file)
  4. compositions.css
  5. utilities.css
  6. blocks.css
  7. exceptions.css
  ─────────────────────────────────────────────────────────────────────────────
*/


/* ══════════════════════════════════════════════════════════════════════════
   COLOUR TOKENS
   Semantic colour roles. Named by WHERE they are used, not WHAT colour they are.
   ══════════════════════════════════════════════════════════════════════════ */

:root {

  /* Backgrounds */
  --color-bg:           var(--stone-100);   /* Page background */
  --color-bg-surface:   var(--white);       /* Cards, panels, raised surfaces */
  --color-bg-subtle:    var(--stone-200);   /* Subtle section backgrounds */
  --color-bg-inverse:   var(--navy-900);    /* Dark sections, footer */
  --color-bg-brand:     var(--navy-800);    /* Brand-coloured panels */
  --color-bg-accent:    var(--gold-50);     /* Accent-coloured highlights */

  /* Foreground / Text */
  --color-text:         var(--navy-900);    /* Primary body text */
  --color-text-muted:   var(--navy-700);    /* Secondary / supporting text */
  --color-text-subtle:  var(--stone-500);   /* Placeholder, disabled, captions */
  --color-text-inverse: var(--white);       /* Text on dark backgrounds */
  --color-text-accent:  var(--gold-500);    /* Accent text, pull quotes */
  --color-text-link:    var(--navy-600);    /* Inline link colour */
  --color-text-link-hover: var(--gold-500); /* Link hover */

  /* Brand */
  --color-brand:        var(--navy-900);    /* Primary brand colour */
  --color-brand-mid:    var(--navy-700);    /* Mid brand — hover states */
  --color-brand-light:  var(--navy-500);    /* Light brand — active states */
  --color-accent:       var(--gold-500);    /* Accent / highlight colour */
  --color-accent-muted: var(--gold-300);    /* Softened accent */
  --color-accent-light: var(--gold-50);     /* Very subtle accent bg */

  /* Borders */
  --color-border:       var(--stone-300);   /* Default border */
  --color-border-strong:var(--stone-400);   /* Stronger border, dividers */
  --color-border-brand: var(--navy-700);    /* Brand-coloured border */
  --color-border-accent:var(--gold-400);    /* Accent border */

  /* States */
  --color-error:        var(--red-500);
  --color-error-bg:     var(--red-100);
  --color-success:      var(--green-500);
  --color-success-bg:   var(--green-100);
  --color-warning:      var(--amber-500);
  --color-warning-bg:   var(--amber-100);

  /* Focus */
  --color-focus:        var(--gold-500);    /* Focus ring colour — do not change */

}


/* ══════════════════════════════════════════════════════════════════════════
   TYPOGRAPHY TOKENS
   All font decisions live here. Components use these — never font stacks directly.
   ══════════════════════════════════════════════════════════════════════════ */

:root {

  /* Font families */
  --font-serif: var(--font-stack-serif);    /* Display, headings, pull quotes */
  --font-sans:  var(--font-stack-sans);     /* Body, UI, forms, nav */
  --font-mono:  var(--font-stack-mono);     /* Code only */

  /* Font sizes — modular scale
     Use --font-size-base (16px) as the anchor.
     Go down for small text, up for headings.
     Named by their semantic size role.
  */
  --font-size-2xs: var(--size-2);     /* 8px  — legal fine print only */
  --font-size-xs:  var(--size-3);     /* 12px — labels, badges */
  --font-size-sm:  var(--size-3-5);   /* 14px — captions, helper text */
  --font-size-base:var(--size-4);     /* 16px — body copy anchor */
  --font-size-lg:  var(--size-5);     /* 20px — large body, lead text */
  --font-size-xl:  var(--size-6);     /* 24px — h4, section intros */
  --font-size-2xl: var(--size-8);     /* 32px — h3 */
  --font-size-3xl: var(--size-10);    /* 40px — h2 */
  --font-size-4xl: var(--size-12);    /* 48px — h1 */
  --font-size-5xl: var(--size-16);    /* 64px — hero display */
  --font-size-6xl: var(--size-24);    /* 96px — large display only */

  /* Font weights */
  --font-weight-light:    300;
  --font-weight-normal:   400;
  --font-weight-medium:   500;
  --font-weight-semibold: 600;
  --font-weight-bold:     700;

  /* Line heights */
  --line-height-none:    1;
  --line-height-tight:   1.15;   /* Display headings */
  --line-height-snug:    1.3;    /* Subheadings */
  --line-height-normal:  1.6;    /* Body copy */
  --line-height-relaxed: 1.75;   /* Long-form text, legal pages */
  --line-height-loose:   2;      /* Spaced lists */

  /* Letter spacing */
  --letter-spacing-tighter: -0.04em;  /* Large serif display only */
  --letter-spacing-tight:   -0.02em;  /* Headings */
  --letter-spacing-normal:   0em;     /* Body */
  --letter-spacing-wide:     0.05em;  /* Uppercase labels */
  --letter-spacing-wider:    0.08em;  /* Small caps */
  --letter-spacing-widest:   0.15em;  /* Eyebrow text, badges */

}


/* ══════════════════════════════════════════════════════════════════════════
   SPACING TOKENS
   Used for padding, margin, and gap. Never use raw values or primitives in
   components — always use a space token.
   ══════════════════════════════════════════════════════════════════════════ */

:root {

  --space-2xs: var(--size-1);     /*  4px — icon padding, micro gaps */
  --space-xs:  var(--size-2);     /*  8px — tight inline spacing */
  --space-sm:  var(--size-3);     /* 12px — compact padding */
  --space-md:  var(--size-4);     /* 16px — base element padding */
  --space-lg:  var(--size-6);     /* 24px — generous element padding */
  --space-xl:  var(--size-8);     /* 32px — component gaps */
  --space-2xl: var(--size-12);    /* 48px — section inner padding */
  --space-3xl: var(--size-16);    /* 64px — section vertical rhythm */
  --space-4xl: var(--size-24);    /* 96px — large section gaps */
  --space-5xl: var(--size-32);    /* 128px — hero / full-bleed spacing */
  --space-6xl: var(--size-40);    /* 160px — maximum section padding */

  /* Named semantic spacers for common patterns */
  --space-section:    var(--space-5xl);   /* Top/bottom padding on page sections */
  --space-component:  var(--space-3xl);   /* Internal padding inside components */
  --space-element:    var(--space-xl);    /* Gap between elements within components */
  --space-inline:     var(--space-md);    /* Horizontal padding on buttons, tags */

}


/* ══════════════════════════════════════════════════════════════════════════
   LAYOUT TOKENS
   Max-widths and layout constraint values.
   ══════════════════════════════════════════════════════════════════════════ */

:root {

  --width-narrow:  42rem;   /* 672px  — article / blog text column */
  --width-content: 72rem;   /* 1152px — standard content width */
  --width-wide:    90rem;   /* 1440px — full-bleed layout width */

  --gutter: var(--space-lg);       /* Page-level horizontal padding (mobile) */
  --gutter-md: var(--space-2xl);   /* Page-level horizontal padding (tablet+) */
  --gutter-lg: var(--space-3xl);   /* Page-level horizontal padding (desktop+) */

}


/* ══════════════════════════════════════════════════════════════════════════
   SHAPE TOKENS (Border Radius)
   ══════════════════════════════════════════════════════════════════════════ */

:root {

  --radius-none: 0;
  --radius-sm:   var(--size-0-5);  /*  2px — subtle rounding */
  --radius-md:   var(--size-1);    /*  4px — standard component radius */
  --radius-lg:   var(--size-2);    /*  8px — cards, panels */
  --radius-xl:   var(--size-4);    /* 16px — large cards */
  --radius-2xl:  var(--size-8);    /* 32px — pill-like elements */
  --radius-full: 9999px;           /* Perfect circle / pill */

}


/* ══════════════════════════════════════════════════════════════════════════
   SHADOW TOKENS
   Used for elevation. Prefer --shadow-sm for most UI elements.
   ══════════════════════════════════════════════════════════════════════════ */

:root {

  --shadow-none: none;
  --shadow-sm:   0 1px 3px hsl(0 0% 0% / 0.06), 0 1px 2px hsl(0 0% 0% / 0.04);
  --shadow-md:   0 4px 6px hsl(0 0% 0% / 0.05), 0 2px 4px hsl(0 0% 0% / 0.06);
  --shadow-lg:   0 10px 15px hsl(0 0% 0% / 0.08), 0 4px 6px hsl(0 0% 0% / 0.05);
  --shadow-xl:   0 20px 25px hsl(0 0% 0% / 0.1), 0 10px 10px hsl(0 0% 0% / 0.04);
  --shadow-2xl:  0 40px 60px hsl(0 0% 0% / 0.15);

  /* Inset / inner shadows */
  --shadow-inset-sm: inset 0 1px 2px hsl(0 0% 0% / 0.06);
  --shadow-inset-md: inset 0 2px 4px hsl(0 0% 0% / 0.1);

  /* Coloured shadows for brand elements */
  --shadow-brand:  0 8px 32px hsl(from var(--navy-900) h s l / 0.25);
  --shadow-accent: 0 4px 16px hsl(from var(--gold-500) h s l / 0.3);

}


/* ══════════════════════════════════════════════════════════════════════════
   MOTION TOKENS
   Used in CSS transitions AND passed to GSAP as JS values.
   Import src/scripts/gsap.ts for GSAP animations — it reads these tokens.

   ALWAYS guard animations with:
   if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
   ══════════════════════════════════════════════════════════════════════════ */

:root {

  /* Durations */
  --duration-instant: 0ms;
  --duration-fast:    150ms;   /* Hover states, micro feedback */
  --duration-normal:  250ms;   /* Standard transitions */
  --duration-slow:    400ms;   /* Page elements, modals */
  --duration-slower:  600ms;   /* Section entrances */
  --duration-crawl:   1000ms;  /* Hero animations, counters */

  /* Easing — CSS cubic-bezier equivalents of GSAP eases */
  --ease-linear:   linear;
  --ease-out:      cubic-bezier(0.0, 0.0, 0.2, 1);   /* Elements entering */
  --ease-in:       cubic-bezier(0.4, 0.0, 1, 1);     /* Elements leaving */
  --ease-inout:    cubic-bezier(0.4, 0.0, 0.2, 1);   /* Bidirectional */
  --ease-spring:   cubic-bezier(0.34, 1.56, 0.64, 1); /* Springy, playful */
  --ease-smooth:   cubic-bezier(0.25, 0.46, 0.45, 0.94); /* Smooth, refined */

}


/* ══════════════════════════════════════════════════════════════════════════
   BREAKPOINT REFERENCE
   These are not custom properties — CSS media queries cannot use var().
   They are documented here for reference only. Use these exact values
   in all media queries throughout the project. Do not invent new breakpoints.

   --bp-sm:  640px   (landscape phone)
   --bp-md:  768px   (tablet)
   --bp-lg:  1024px  (small desktop)
   --bp-xl:  1280px  (standard desktop)
   --bp-2xl: 1536px  (large desktop)

   Usage in CSS:
   @media (min-width: 768px) { ... }
   @media (min-width: 1024px) { ... }
   ══════════════════════════════════════════════════════════════════════════ */
```

---

## STEP 3 — Update global.css

Replace `src/styles/global.css` entirely with:

```css
/*
  GLOBAL STYLESHEET — ENTRY POINT
  ─────────────────────────────────────────────────────────────────────────────
  Imported by BaseLayout.astro. This file controls import order.
  Import order matters — each layer depends on the one above it.

  IMPORT ORDER (mandatory — do not rearrange):
  1. reset        — browser normalisation, references semantic tokens
  2. theme        — Layer 1 primitives (swap this file to retheme the site)
  3. tokens       — Layer 2 semantic tokens (references primitives)
  4. compositions — CUBE CSS layout patterns (references semantic tokens)
  5. utilities    — CUBE CSS single-job classes (references semantic tokens)
  6. blocks       — CUBE CSS global element styles (h1-h6, a, p, lists)
  7. exceptions   — Overrides, one-offs, last resort (keep minimal)

  DO NOT:
  - Import Tailwind or any CSS framework
  - Add component styles here (they live in component <style> blocks)
  - Use raw values — all values must trace to a token
  - Add more than one theme import at a time
  ─────────────────────────────────────────────────────────────────────────────
*/

@import './reset.css';
@import './themes/theme-cream-navy.css';
@import './tokens.css';
@import './compositions.css';
@import './utilities.css';
@import './blocks.css';
@import './exceptions.css';
```

---

## STEP 4 — Rewrite reset.css

Replace `src/styles/reset.css` entirely with a modern reset that uses semantic tokens. The reset must not contain any raw values — everything references a token.

```css
/*
  RESET — BROWSER NORMALISATION
  ─────────────────────────────────────────────────────────────────────────────
  References semantic tokens from tokens.css.
  Do not add component styles here.
  Do not hardcode any values — use tokens.
  ─────────────────────────────────────────────────────────────────────────────
*/

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 100%;
  -webkit-text-size-adjust: 100%;
  scroll-behavior: smooth;
  hanging-punctuation: first last;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

body {
  font-family: var(--font-sans);
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-normal);
  line-height: var(--line-height-normal);
  color: var(--color-text);
  background-color: var(--color-bg);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  min-height: 100dvh;
}

img, picture, video, canvas, svg {
  display: block;
  max-width: 100%;
}

input, button, textarea, select {
  font: inherit;
  color: inherit;
}

p, h1, h2, h3, h4, h5, h6 {
  overflow-wrap: break-word;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-serif);
  font-weight: var(--font-weight-bold);
  line-height: var(--line-height-tight);
  letter-spacing: var(--letter-spacing-tight);
  color: var(--color-text);
}

a {
  color: var(--color-text-link);
  text-decoration-color: transparent;
  text-underline-offset: 0.2em;
  transition: color var(--duration-fast) var(--ease-out),
              text-decoration-color var(--duration-fast) var(--ease-out);
}

a:hover {
  color: var(--color-text-link-hover);
  text-decoration-color: currentColor;
}

a:focus-visible {
  outline: 2px solid var(--color-focus);
  outline-offset: 3px;
  border-radius: var(--radius-sm);
}

button {
  cursor: pointer;
  border: none;
  background: none;
}

button:focus-visible,
[tabindex]:focus-visible {
  outline: 2px solid var(--color-focus);
  outline-offset: 3px;
}

ul, ol {
  list-style: none;
}

table {
  border-collapse: collapse;
}

hr {
  border: none;
  border-top: 1px solid var(--color-border);
}

::selection {
  background-color: var(--color-accent-light);
  color: var(--color-text);
}
```

---

## STEP 5 — Create compositions.css

Create `src/styles/compositions.css`

Compositions are layout patterns only — they handle how elements are arranged. No colours, no typography, no visual styling.

```css
/*
  COMPOSITIONS — CUBE CSS LAYER 3
  ─────────────────────────────────────────────────────────────────────────────
  Compositions handle layout structure only.
  No colour, no typography, no decoration here.

  These classes are CUBE CSS "C" — they define the skeleton.
  Every composition uses semantic spacing tokens.

  Usage: Add these classes to HTML elements to apply layout.
  Example: <section class="flow wrapper">...</section>
  ─────────────────────────────────────────────────────────────────────────────
*/


/* ── WRAPPER ──────────────────────────────────────────────────────────────
   Centers content with consistent horizontal padding.
   Use on sections and page-level containers.
──────────────────────────────────────────────────────────────────────── */

.wrapper {
  width: 100%;
  max-width: var(--width-content);
  margin-inline: auto;
  padding-inline: var(--gutter);
}

@media (min-width: 768px) {
  .wrapper {
    padding-inline: var(--gutter-md);
  }
}

@media (min-width: 1024px) {
  .wrapper {
    padding-inline: var(--gutter-lg);
  }
}

.wrapper--narrow {
  max-width: var(--width-narrow);
}

.wrapper--wide {
  max-width: var(--width-wide);
}


/* ── STACK ────────────────────────────────────────────────────────────────
   Vertical flow with consistent gap between children.
   Children are not aware of the stack — the stack controls spacing.
──────────────────────────────────────────────────────────────────────── */

.stack {
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

.stack > * + * {
  margin-top: var(--space-element);
}

.stack[data-gap="xs"]  > * + * { margin-top: var(--space-xs); }
.stack[data-gap="sm"]  > * + * { margin-top: var(--space-sm); }
.stack[data-gap="md"]  > * + * { margin-top: var(--space-md); }
.stack[data-gap="lg"]  > * + * { margin-top: var(--space-lg); }
.stack[data-gap="xl"]  > * + * { margin-top: var(--space-xl); }
.stack[data-gap="2xl"] > * + * { margin-top: var(--space-2xl); }
.stack[data-gap="3xl"] > * + * { margin-top: var(--space-3xl); }


/* ── CLUSTER ──────────────────────────────────────────────────────────────
   Horizontal group of items that wraps naturally.
   Use for: tag lists, icon+text pairs, button groups.
──────────────────────────────────────────────────────────────────────── */

.cluster {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-sm);
  align-items: center;
}

.cluster[data-gap="xs"]  { gap: var(--space-xs); }
.cluster[data-gap="md"]  { gap: var(--space-md); }
.cluster[data-gap="lg"]  { gap: var(--space-lg); }
.cluster[data-gap="xl"]  { gap: var(--space-xl); }

.cluster[data-align="start"]   { align-items: flex-start; }
.cluster[data-align="end"]     { align-items: flex-end; }
.cluster[data-align="stretch"] { align-items: stretch; }

.cluster[data-justify="center"]  { justify-content: center; }
.cluster[data-justify="between"] { justify-content: space-between; }
.cluster[data-justify="end"]     { justify-content: flex-end; }


/* ── GRID ─────────────────────────────────────────────────────────────────
   Responsive auto-fit grid. Items flow to fill available space.
   Use for: service cards, team members, feature lists.
──────────────────────────────────────────────────────────────────────── */

.grid {
  display: grid;
  gap: var(--space-xl);
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 18rem), 1fr));
}

.grid[data-gap="sm"]  { gap: var(--space-sm); }
.grid[data-gap="md"]  { gap: var(--space-md); }
.grid[data-gap="lg"]  { gap: var(--space-lg); }
.grid[data-gap="2xl"] { gap: var(--space-2xl); }

.grid[data-min="12rem"] { grid-template-columns: repeat(auto-fit, minmax(min(100%, 12rem), 1fr)); }
.grid[data-min="16rem"] { grid-template-columns: repeat(auto-fit, minmax(min(100%, 16rem), 1fr)); }
.grid[data-min="20rem"] { grid-template-columns: repeat(auto-fit, minmax(min(100%, 20rem), 1fr)); }
.grid[data-min="24rem"] { grid-template-columns: repeat(auto-fit, minmax(min(100%, 24rem), 1fr)); }


/* ── SIDEBAR ──────────────────────────────────────────────────────────────
   Two-column layout where one column has a fixed minimum width.
   Use for: aside + content, image + text, icon + description.
──────────────────────────────────────────────────────────────────────── */

.sidebar {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-xl);
}

.sidebar > :first-child {
  flex-basis: 20rem;
  flex-grow: 1;
}

.sidebar > :last-child {
  flex-basis: 0;
  flex-grow: 999;
  min-width: min(50%, 30rem);
}


/* ── SECTION ──────────────────────────────────────────────────────────────
   Full-width page section with consistent vertical rhythm.
   Use on every <section> element in pages.
──────────────────────────────────────────────────────────────────────── */

.section {
  padding-block: var(--space-section);
}

.section[data-size="sm"] { padding-block: var(--space-3xl); }
.section[data-size="lg"] { padding-block: var(--space-6xl); }


/* ── FLOW ─────────────────────────────────────────────────────────────────
   Adds consistent margin-top between flow content (prose).
   Use inside article bodies, legal pages, rich text content.
──────────────────────────────────────────────────────────────────────── */

.flow > * + * {
  margin-top: var(--space-md);
}

.flow[data-gap="sm"] > * + * { margin-top: var(--space-sm); }
.flow[data-gap="lg"] > * + * { margin-top: var(--space-lg); }
.flow[data-gap="xl"] > * + * { margin-top: var(--space-xl); }
```

---

## STEP 6 — Create utilities.css

Create `src/styles/utilities.css`

```css
/*
  UTILITIES — CUBE CSS LAYER 4
  ─────────────────────────────────────────────────────────────────────────────
  Single-job classes. Each class does exactly one thing.
  Use these to handle exceptions without writing new CSS.

  Do not add multi-property classes here.
  Do not add visual styling (colour, typography) unless it is a single property.
  ─────────────────────────────────────────────────────────────────────────────
*/


/* ── ACCESSIBILITY ────────────────────────────────────────────────────── */

.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}

.skip-link {
  position: absolute;
  top: var(--space-sm);
  left: var(--space-sm);
  z-index: 9999;
  padding: var(--space-sm) var(--space-md);
  background: var(--color-brand);
  color: var(--color-text-inverse);
  border-radius: var(--radius-md);
  font-weight: var(--font-weight-semibold);
  transform: translateY(-200%);
  transition: transform var(--duration-fast) var(--ease-out);
}

.skip-link:focus {
  transform: translateY(0);
}


/* ── TEXT UTILITIES ───────────────────────────────────────────────────── */

.text-center  { text-align: center; }
.text-left    { text-align: left; }
.text-right   { text-align: right; }

.text-balance { text-wrap: balance; }
.text-pretty  { text-wrap: pretty; }

.text-muted   { color: var(--color-text-muted); }
.text-subtle  { color: var(--color-text-subtle); }
.text-accent  { color: var(--color-text-accent); }
.text-inverse { color: var(--color-text-inverse); }

.text-sm      { font-size: var(--font-size-sm); }
.text-lg      { font-size: var(--font-size-lg); }
.text-xl      { font-size: var(--font-size-xl); }

.font-serif   { font-family: var(--font-serif); }
.font-sans    { font-family: var(--font-sans); }

.font-medium  { font-weight: var(--font-weight-medium); }
.font-bold    { font-weight: var(--font-weight-bold); }

.uppercase    { text-transform: uppercase; letter-spacing: var(--letter-spacing-wide); }
.tracking-wide { letter-spacing: var(--letter-spacing-wide); }
.tracking-widest { letter-spacing: var(--letter-spacing-widest); }


/* ── DISPLAY UTILITIES ────────────────────────────────────────────────── */

.block         { display: block; }
.inline-block  { display: inline-block; }
.flex          { display: flex; }
.grid-display  { display: grid; }
.hidden        { display: none; }

.items-center  { align-items: center; }
.items-start   { align-items: flex-start; }
.justify-center{ justify-content: center; }
.justify-between { justify-content: space-between; }

.w-full        { width: 100%; }
.h-full        { height: 100%; }
.min-h-screen  { min-height: 100dvh; }

.relative      { position: relative; }
.absolute      { position: absolute; }
.overflow-hidden { overflow: hidden; }


/* ── COLOUR UTILITIES ─────────────────────────────────────────────────── */

.bg-surface    { background-color: var(--color-bg-surface); }
.bg-subtle     { background-color: var(--color-bg-subtle); }
.bg-inverse    { background-color: var(--color-bg-inverse); }
.bg-brand      { background-color: var(--color-bg-brand); }
.bg-accent     { background-color: var(--color-bg-accent); }


/* ── SPACING UTILITIES ────────────────────────────────────────────────── */

.mt-auto  { margin-top: auto; }
.ml-auto  { margin-left: auto; }
.mx-auto  { margin-inline: auto; }

/* ── BORDER UTILITIES ─────────────────────────────────────────────────── */

.border-top    { border-top: 1px solid var(--color-border); }
.border-bottom { border-bottom: 1px solid var(--color-border); }
.rounded-md    { border-radius: var(--radius-md); }
.rounded-lg    { border-radius: var(--radius-lg); }
```

---

## STEP 7 — Create blocks.css

Create `src/styles/blocks.css`

```css
/*
  BLOCKS — CUBE CSS LAYER 5
  ─────────────────────────────────────────────────────────────────────────────
  Global styles for HTML elements. Sets the typographic baseline.
  These styles apply to elements without any class.

  This is where the "design" of the system lives at the element level.
  Every heading, paragraph, list, and blockquote style goes here.

  Components may override these with scoped styles in their own <style> blocks.
  ─────────────────────────────────────────────────────────────────────────────
*/


/* ── HEADINGS ─────────────────────────────────────────────────────────── */

h1 {
  font-size: clamp(var(--font-size-3xl), 5vw, var(--font-size-5xl));
  font-weight: var(--font-weight-bold);
  line-height: var(--line-height-tight);
  letter-spacing: var(--letter-spacing-tight);
}

h2 {
  font-size: clamp(var(--font-size-2xl), 4vw, var(--font-size-3xl));
  font-weight: var(--font-weight-bold);
  line-height: var(--line-height-tight);
  letter-spacing: var(--letter-spacing-tight);
}

h3 {
  font-size: clamp(var(--font-size-xl), 3vw, var(--font-size-2xl));
  font-weight: var(--font-weight-semibold);
  line-height: var(--line-height-snug);
}

h4 {
  font-size: var(--font-size-xl);
  font-weight: var(--font-weight-semibold);
  line-height: var(--line-height-snug);
}

h5 {
  font-size: var(--font-size-lg);
  font-weight: var(--font-weight-medium);
}

h6 {
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-medium);
  letter-spacing: var(--letter-spacing-wide);
  text-transform: uppercase;
}


/* ── BODY TEXT ────────────────────────────────────────────────────────── */

p {
  max-width: 65ch;
  line-height: var(--line-height-normal);
}

p.lead {
  font-size: var(--font-size-lg);
  line-height: var(--line-height-relaxed);
  color: var(--color-text-muted);
}

strong { font-weight: var(--font-weight-bold); }
em     { font-style: italic; }

small {
  font-size: var(--font-size-sm);
  color: var(--color-text-subtle);
}


/* ── LISTS ────────────────────────────────────────────────────────────── */

/* Content lists (inside .flow or article body) */
.flow ul,
.flow ol {
  padding-left: var(--space-lg);
}

.flow ul { list-style: disc; }
.flow ol { list-style: decimal; }

.flow li + li {
  margin-top: var(--space-xs);
}


/* ── BLOCKQUOTE ───────────────────────────────────────────────────────── */

blockquote {
  border-left: 3px solid var(--color-accent);
  padding-left: var(--space-lg);
  margin-left: 0;
  font-family: var(--font-serif);
  font-size: var(--font-size-xl);
  font-style: italic;
  line-height: var(--line-height-relaxed);
  color: var(--color-text-muted);
}


/* ── HORIZONTAL RULE ──────────────────────────────────────────────────── */

hr {
  margin-block: var(--space-2xl);
}


/* ── CODE ─────────────────────────────────────────────────────────────── */

code {
  font-family: var(--font-mono);
  font-size: var(--font-size-sm);
  background: var(--color-bg-subtle);
  padding: var(--space-2xs) var(--space-xs);
  border-radius: var(--radius-sm);
}
```

---

## STEP 8 — Create exceptions.css

Create `src/styles/exceptions.css`

```css
/*
  EXCEPTIONS — CUBE CSS LAYER 6
  ─────────────────────────────────────────────────────────────────────────────
  Last resort. One-off overrides that don't belong anywhere else.

  RULES:
  - Keep this file as small as possible
  - Every entry here should have a comment explaining WHY it is an exception
  - If you find yourself adding many rules here, a composition or utility is missing
  - Never use !important unless absolutely unavoidable, and document why
  ─────────────────────────────────────────────────────────────────────────────
*/

/* No exceptions yet. Add sparingly with a comment explaining each one. */
```

---

## STEP 9 — Load Fonts in BaseLayout.astro

In `src/layouts/BaseLayout.astro`, add the following inside the `<head>` tag, before the global.css import:

```astro
{/* Fontsource — loaded via @fontsource package, not CDN */}
import '@fontsource/cormorant-garamond/400.css';
import '@fontsource/cormorant-garamond/400-italic.css';
import '@fontsource/cormorant-garamond/600.css';
import '@fontsource/cormorant-garamond/700.css';
import '@fontsource/dm-sans/400.css';
import '@fontsource/dm-sans/500.css';
import '@fontsource/dm-sans/700.css';
```

If `@fontsource/cormorant-garamond` and `@fontsource/dm-sans` are not yet installed, install them:

```bash
npm install @fontsource/cormorant-garamond @fontsource/dm-sans
```

Then uninstall the old font packages that were in package.json but never used:
```bash
npm uninstall @fontsource/merriweather @fontsource/open-sans
```

---

## STEP 10 — Verify

Run the following and report results:

```bash
npm run build
```

The build must:
- Complete without errors
- Produce no TypeScript errors
- All 17 theme files in `src/styles/themes/` should still exist and be untouched (they are reserved for future use — only `theme-cream-navy.css` is active)

Also verify manually:
- Open the dev server with `npm run dev`
- Visit the home page in a browser
- Confirm that body text renders in DM Sans (not a browser default font)
- Confirm that any heading renders in Cormorant Garamond (not a browser default serif)
- Confirm the page background is the cream/stone colour, not white
- Open browser dev tools and confirm `--color-text`, `--font-sans`, `--space-section` are all defined on `:root`

Report any errors or failures in full.

---

## What This Stage Does NOT Do

- Does not build any components
- Does not style any pages
- Does not modify any TypeScript or config files
- Does not create new pages
- Does not touch the Mary Molloy content

Components are Stage 4. This stage is foundation only.


# Token System — References, Sources, and Attribution

This document records where every decision in the token system came from.
Each decision is either directly sourced from a real production design system,
synthesised from multiple real systems, or an original decision made for this
specific use case (solicitor websites). Nothing here is invented without basis.

---

## Real Systems Referenced

### Material Design 3 — Google
**Source:** https://m3.material.io/foundations/design-tokens and https://material-web.dev/theming/material-theming/

The three-layer architecture (reference → system → component) comes directly from Material Design 3.
Google uses `--md-ref-*` for primitives and `--md-sys-*` for semantic tokens.
We adopted the concept and dropped the prefix — unnecessary complexity for a solo developer.

Specific decisions taken from MD3:
- The idea that semantic tokens describe a **role**, not an appearance
- Component tokens scoped to the component selector rather than `:root`
- Theme switching as a primitive-layer swap only, with semantic and component tokens unchanged
- The `on-` naming convention (e.g. `on-primary`) inspired `--color-text-inverse`

### Shopify Polaris
**Source:** https://github.com/Shopify/polaris-tokens and https://polaris.shopify.com/tokens/color

Specific decisions taken from Polaris:
- **Numeric scale (100, 200... 900) for colour brightness** — Polaris v10 migrated to this from named scales (lightest, lighter, light etc.) precisely because numeric scales are more flexible and scalable. We use the same 50–900 range.
- **Numeric scale for font sizes** — Polaris uses `--p-font-size-100` as base with `--p-font-size-75` below and `--p-font-size-200` above. We adapted this to semantic names (base, lg, xl) rather than numbers because semantic names read better in component CSS.
- The principle of **semantic tokens describing use, not value** — Polaris documentation explicitly states "tokens are a method of applying colour in a consistent and meaningful way".

### GitHub Primer
**Source:** https://primer.style/product/primitives/token-names/ and https://github.com/primer/primitives

Specific decisions taken from Primer:
- **Category-first naming** — Primer uses `bgColor-default`, `fgColor-muted`, `borderColor-accent`. We adapted this to `--color-bg`, `--color-text-muted`, `--color-border` — same thinking, slightly shorter names.
- **The base/functional split** — Primer separates "base tokens" (raw values, like `base-color-green-5`) from "functional tokens" (role-based, like `bgColor-inset`). This is our primitives/semantic split.
- The `muted`, `subtle`, `inverse` variant vocabulary for text colours (`--color-text-muted`, `--color-text-subtle`, `--color-text-inverse`) is drawn directly from Primer's naming vocabulary.

### Adobe Spectrum
**Source:** https://spectrum.adobe.com/page/design-tokens/ and https://github.com/adobe/spectrum-css

Specific decisions taken from Spectrum:
- **Colour family + brightness scale naming for primitives** — Spectrum uses `--spectrum-gray-100` through `--spectrum-gray-900`. We use `--navy-900`, `--gold-500` etc. Same pattern, applied to brand-specific colour families rather than generic colour names.
- **The 4px base unit for spacing** — Spectrum's spacing system is built on a 4px grid. Our `--size-1: 0.25rem` (4px) base unit follows this directly.
- **Motion token vocabulary** — `duration` and `ease` as the two motion categories comes from Spectrum's motion token structure.
- The **three-tier shadow scale** (sm, md, lg) with increasing opacity values follows Spectrum's elevation model.

### W3C Design Tokens Community Group (DTCG)
**Source:** https://www.designtokens.org/ — First stable specification released October 2025.

The DTCG specification reached its first stable version (2025.10) in October 2025.
We align with its core principles:
- Tokens as named, platform-agnostic values
- Separation of primitive (reference) tokens from semantic (alias) tokens
- Kebab-case naming for CSS custom properties
- The JSON token format with `$value`, `$type`, `$description` keys (for future tooling compatibility)

We do not implement the full DTCG JSON pipeline (Style Dictionary, Tokens Studio) as it adds unnecessary build complexity for a single-developer, web-only project. The CSS custom property output is what the spec would produce anyway.

### Nathan Curtis — EightShapes (naming theory)
**Source:** https://medium.com/eightshapes-llc/naming-tokens-in-design-systems-9e86c7444676

The naming framework paper by Nathan Curtis is the most cited academic reference on token naming.
Specific decisions drawn from this:
- **Avoid homonyms** — `font` not `type` or `typography` (type is ambiguous, typography is too long)
- **Category as the first segment** — `--color-*`, `--font-*`, `--space-*` not `--primary-color` or `--spacing-lg`
- The vocabulary choices: `space` over `spacing`, `font` over `type`, `radius` over `border-radius`

### CUBE CSS — Andy Bell
**Source:** https://piccalil.li/blog/cube-css/ and https://cube.fyi/

The file structure (compositions, utilities, blocks, exceptions) comes directly from CUBE CSS methodology.
Specific decisions drawn from CUBE:
- **Compositions handle layout only** — no colour or typography in `.wrapper`, `.stack`, `.grid` etc.
- **Data attributes for variants** — `data-gap="lg"`, `data-size="sm"` on compositions instead of modifier classes
- **Blocks are minimal** — global element styles in `blocks.css` should be small because the token system did most of the work already
- The `.flow` composition (margin-top between siblings) is a direct CUBE CSS pattern
- **Exceptions file is last resort** — CUBE explicitly discourages heavy use of exceptions

---

## Decisions Synthesised From Multiple Systems

These decisions don't come from a single system — they are the intersection of what multiple systems independently arrived at.

### Three-layer token architecture
Independently used by MD3, Polaris, Primer, and Spectrum. All four arrived at the same structure:
raw values → semantic roles → component overrides.
The specific names we use (primitive, semantic, component) are the most common vocabulary across these systems.

### Numeric brightness scale (50–900) for colours
Both Shopify Polaris and Adobe Spectrum use this independently. Tailwind also uses it.
The fact that three unrelated large-scale design systems landed on the same pattern is strong evidence it is the right choice.

### 4px base unit for spacing
Adobe Spectrum, Material Design, and most design tools (Figma's default grid) use 4px as the base unit.
The specific sizes (0.25rem, 0.5rem, 1rem, 2rem...) follow a powers-of-2 doubling pattern which both Spectrum and Open Props (Adam Argyle, Google Chrome) use.

### Semantic colour vocabulary
The words `muted`, `subtle`, `inverse`, `surface`, `brand`, `accent` appear in multiple real systems:
- `surface` — MD3 uses this extensively
- `muted` / `subtle` — GitHub Primer uses both
- `inverse` — MD3 uses `on-` prefix, Primer uses `inverse`, we use `inverse` for clarity
- `brand` / `accent` — common across most systems

### CSS cubic-bezier easing tokens
The four easing curves (ease-out, ease-in, ease-inout, ease-spring) are the CSS equivalents of GSAP's named easings. This alignment means GSAP animations and CSS transitions feel identical in motion character across the site. The specific cubic-bezier values come from Material Design 3's motion specification.

---

## Decisions Made for This Project Specifically

These decisions have no single source system — they are original choices made for the solicitor website context.

### Font pairing: Cormorant Garamond + DM Sans
No design system specifies these — this is an original typographic decision.
**Rationale:** Cormorant Garamond is a refined, authoritative serif with strong legal and financial associations. DM Sans is highly legible, neutral, and modern without being cold. The contrast between the two creates the "trusted professional" tone appropriate for Irish solicitor websites.
**Alternative for future clients:** Libre Baskerville + Inter, or Playfair Display + Source Sans 3.

### Colour palette: Navy + Gold + Stone
No design system specifies these — this is an original palette decision.
**Rationale:** Navy communicates authority and trustworthiness (used heavily in legal and financial sectors). Gold communicates quality and premium positioning without being flashy. Stone (warm neutral) prevents the palette from feeling cold or corporate. This combination appears frequently in the branding of established legal firms.

### --color-focus uses gold accent
Most systems use a high-contrast blue for focus rings. We use `--color-accent` (gold) deliberately:
- Gold has sufficient contrast against the navy and stone surfaces
- It reinforces brand colour rather than introducing a foreign UI colour
- It prevents the site from looking generically "accessible blue"

### Named semantic spacers (--space-section, --space-component, --space-element, --space-inline)
These four contextual spacers sit on top of the scale tokens.
No system names spacers this way — this is an original abstraction.
**Rationale:** On solicitor websites, the same scale token might feel right in one section and wrong in another because the visual weight of legal content is heavy. Giving sections, components, elements, and inline items their own named token means a designer can adjust all section spacing globally in one change.

### Breakpoints as CSS comments (not custom properties)
CSS media queries cannot use `var()` — this is a browser limitation, not a design decision.
Documenting the breakpoints in `tokens.css` as comments rather than inventing workarounds (PostCSS, container queries only, etc.) is a pragmatic choice to keep the system simple and Claude-readable.

### Coloured shadow tokens (--shadow-brand, --shadow-accent)
Standard design systems only define greyscale shadows. We added two brand-coloured shadows.
**Rationale:** On solicitor hero sections and CTA panels, a subtle navy shadow on a navy card, or a gold glow on an accent element, creates depth that feels deliberate rather than generic. Used sparingly.

---

## What Was Deliberately Left Out

### Style Dictionary / Tokens Studio pipeline
Many enterprise systems use Style Dictionary (Amazon) or Tokens Studio (Figma plugin) to transform JSON token files into CSS. We deliberately skip this for two reasons:
1. It adds a build step that Claude in terminal must understand and maintain
2. The output is CSS custom properties — which we write directly anyway
If this template grows to require Figma parity or multi-platform distribution, Style Dictionary can be added later without changing the token names.

### Dark mode tokens
GitHub Primer and MD3 both define parallel dark mode token sets. We do not.
**Rationale:** Solicitor websites rarely implement dark mode. The 17 theme files already give us a colour-switching mechanism. If dark mode is needed for a specific client, the existing theme architecture supports it — add a `theme-[client]-dark.css` that overrides the same primitives.

### Motion: prefer-reduced-motion token
We chose not to create a `--duration-reduced: 0ms` token. Instead the reduced-motion guard is implemented in `reset.css` at the browser level and in component GSAP code.
**Rationale:** A token for `0ms` would be semantically confusing and might be skipped in component code. A system-level override is more reliable.